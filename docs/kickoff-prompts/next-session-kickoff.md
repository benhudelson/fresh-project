# fresh-project — Next-session kickoff

## Anchor
- Prior session: 2026-07-09 → 2026-07-13. Ground-truth commit: **`ab3a31b`** (`main`, pushed to origin).
- Repo: `/Users/benhudelson/Documents/backup/fresh-project` · branch `main` · **working tree clean**.
- Commit range this session: `73c2ab8..ab3a31b`.
- No prior kickoff — this is the first for this repo.

## Hard operating rules (from global `~/.claude/CLAUDE.md` + this repo's own pattern)
- Don't chain `&&`/`;` commands that are individually allowed — use separate parallel Bash calls. cwd persists between calls; no `cd` prefix.
- **Tables over prose** for any parallel-structure content (the `step-table` skill is the authority).
- Ask, don't assume · simplest solution first · don't touch unrelated code · flag uncertainty · propose a clearly-better approach before implementing.
- **Scope the subagent model to the task**: high-reasoning → Opus, ordinary/complicated → Sonnet, trivial mechanical → Haiku. (Added to global `~/.claude/CLAUDE.md` this session.)
- **Attribution ban** — no `Co-Authored-By` / "Generated by Claude" footers in commits (this repo's core principle; this session's commits omit them).
- **Commit/push only when asked; branch-first on the default branch.** This session branched per change, `--ff-only` merged to `main`, pushed, deleted the branch. (Kickoff commits are the exception — they go straight to `main` per the handoff skill.)

## What landed this session (verified, all pushed)
- **README pattern sync** (`6f8a0a4`) — `README.md` (the pattern doc) brought current with the skills + coachme standards: added `worktree-handoff` + `step-table` skill contracts; refreshed `handoff-kickoff` (continuous-learning sweep, prune-stale, push-to-`main`) and `resume` (branch-aware routing, index/flag-stale); added operating standards (tables-over-prose, hand-runnable-commands, subagent model-scoping, agent-first testing, automated PR-review triage flagged **requires setup**); codified the utility-vs-workflow skill split.
- **Vendored skills via `git subtree`** (`220e0a1`, `56d2b6a`, `fc18aea`) — `.claude/skills/` now ships the 4 utility skills (`handoff-kickoff`, `resume`, `step-table`, `worktree-handoff`) from `github.com/benhudelson/skills`. Dropped `pursue` + CoachME-specific `publish-spec`/`roadmap-phase-kickoff` + a stray coachme kickoff.
- **Upstream skills repo cleaned + pushed** — `github.com/benhudelson/skills` `ac26685 → 62b7555` (allowlist `.gitignore` restricting the repo to the 4 utility skills). Then dropped the now-stale grab-bag caveats from the README (`ab3a31b`).
- **Global directive** — added "Scope the subagent model to the task" to `~/.claude/CLAUDE.md` (cross-project rule, not part of this repo).

## In-flight at handoff
- **One open offer, waiting on Ben:** physically delete `pursue` / `publish-spec` / `roadmap-phase-kickoff` / `docs` from `~/.claude/skills/` on disk. The upstream cleanup used an *allowlist `.gitignore`*, so those dirs are now untracked but **still physically present** — so `pursue` still loads as a global skill on Ben's machine. Ben hasn't answered whether to delete them from disk. Cross-cutting housekeeping, not a fresh-project decision; harmless if left.

## Open work (carry-forward)
- Resolve the disk-cleanup offer above (or drop it — `pursue` loading globally is harmless).
- *(Optional, not requested)* This repo has no root `CLAUDE.md`, `docs/` (beyond this kickoff), or memory dir — by design it's a pattern **document**, not a scaffolded project. Don't build those out unless Ben wants fresh-project to become a live project.

## Closed / superseded this session
- Removed the README's "upstream is a grab-bag / re-remove after pulling" caveats — upstream is now four-only, so `subtree add`/`pull` need no post-cleanup.
- Corrected an earlier wrong premise (that `publish-spec`/`roadmap-phase-kickoff` weren't in the skills repo — they were, in CoachME form; now excluded from the vendored set).

## Out of scope for next session
- Scaffolding fresh-project into a live project (`CLAUDE.md`/`docs/`/`specs/`). It's a template doc by design.
- Genericizing `publish-spec`/`roadmap-phase-kickoff` for the shared repo — they're intentionally project-authored per the pattern.

## Context load order (newest canonical first)
1. `README.md` — the pattern doc; source of truth for *what this repo is*.
2. `~/.claude/CLAUDE.md` — global operating rules (now includes model-scoping).
3. `.claude/skills/` — the 4 vendored utility skills.
4. This kickoff.

## Standing instructions for the next agent
> **Always update or remove memory the same turn an item changes state.** When any tracked item changes state (a commit lands, a decision is made, a blocker is resolved, an ADR is filed, an item is dropped or deferred), update the relevant memory file inline in the same turn. **When an item fully closes, *delete* the entry — do not leave "done" or "resolved" markers; closed entries accumulate and obscure what's actually open.** Cite the commit SHA / Linear ID / ADR slug for the state change in your conversation reply, not in the memory entry. If the change invalidates this kickoff prompt's "what landed" or "open work" sections, flag it to Ben rather than silently editing the kickoff — kickoffs are historical artifacts; memory is the live view.

## START HERE
This session's work is fully shipped and the tree is clean — there is no pending build work. The only loose thread is the one-line question in **In-flight**: whether to physically delete the untracked `pursue`/etc. dirs from `~/.claude/skills/` on disk.

Recommended openers:
1. **Answer the disk-cleanup offer** — delete the untracked skill dirs from `~/.claude/skills/`, or leave them (harmless).
2. Something new.

Wait for Ben's answer. Do not start work until he picks a topic.
